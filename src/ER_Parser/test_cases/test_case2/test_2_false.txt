#Submission which has correct answers but also parts which are clearly wrong 
#The test score should be partial (e.g., 60-70%)

//Tables
[doctor]              # Correct
[Patient]             # Correct
[Drug]                # Wrong name: Medicine -> Drug
[Hospital]            # Extra, incorrect table

//Relations 
(prescribes, ([doctor, 1, n], [Patient, 1, n]), ([Drug, 0, n]))      # Mostly correct, but Drug instead of Medicine
(works_at, ([doctor, 1, 1], [Hospital, 1, n]), ())                   # Extra, incorrect relation